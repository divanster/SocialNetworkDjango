// frontend/src/types/user.ts

export interface User {
  id: string; // Assuming UUID is represented as string
  username: string;
  full_name: string;
  profile_picture: string;
  // Add any additional fields as required
}
